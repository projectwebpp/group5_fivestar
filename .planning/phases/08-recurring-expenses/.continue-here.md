---
context: phase
phase: 08-recurring-expenses
task: 0
total_tasks: 3
status: not_started
last_updated: 2026-05-13T16:28:25.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints identified for this phase — all plans were written from fresh context with no execution failures._

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Silent processRecurring failure | If processRecurring() throws, it must be caught and swallowed — never let a template processing error return a 500 on GET /expenses | blocking | Wrap processRecurring() call in try/catch in ExpenseController::index(); log error but continue |
| Multiple entries per trigger | Do NOT create more than 1 entry per template per GET /expenses call — only the most recent missed period | advisory | Check: if today >= next_due, create 1 entry and update last_created_date; do not loop |

<current_state>
Phase 8 (recurring-expenses) is fully planned — 3 plans created (08-01, 08-02, 08-03) across 3 waves. No code has been written yet. All context is gathered and decisions are locked in 08-CONTEXT.md. Ready to execute.
</current_state>

<completed_work>
- Phase context gathered (08-CONTEXT.md)
- Phase discussed (08-DISCUSSION-LOG.md)
- Research completed (08-RESEARCH.md)
- Pattern mapping done (08-PATTERNS.md)
- Validation criteria defined (08-VALIDATION.md)
- All 3 plans written and committed (08-01-PLAN.md, 08-02-PLAN.md, 08-03-PLAN.md)
</completed_work>

<remaining_work>
- Task 1 (08-01, Wave 1): Backend — migration, RecurringExpense model, RecurringExpenseController, routes, processRecurring() in ExpenseController::index(), Feature tests
- Task 2 (08-02, Wave 2, depends on 08-01): Frontend types (recurring.ts) + API client (api/recurring.ts)
- Task 3 (08-03, Wave 3, depends on 08-01 + 08-02): RecurringPage.tsx, App.tsx routing + nav link, nav sync in ExpensesPage/AnalyticsPage/BudgetPage
</remaining_work>

<decisions_made>
- Auto-creation on-request: processRecurring() called at start of ExpenseController::index()
- One entry per template per trigger (D-03) — avoids flooding after long absence
- Deduplication via last_created_date on template row (D-02)
- Three frequencies only: daily, weekly, monthly (D-08)
- RecurringPage mirrors BudgetPage: table + inline collapsible form above (D-05/D-06)
- Indefinite recurrence — no end_date column, user deletes template to stop (D-11)
- DB schema: recurring_expenses(id, user_id, category_id, description, amount DECIMAL(10,2), currency VARCHAR(3), frequency ENUM('daily','weekly','monthly'), start_date DATE, last_created_date DATE nullable, timestamps)
</decisions_made>

<blockers>
- None blocking execution
- Non-blocking: php artisan migrate must be run on Railway MySQL after 08-01 executes
</blockers>

## Required Reading (in order)

1. `.planning/phases/08-recurring-expenses/08-CONTEXT.md` — all design decisions (D-01 through D-11); authoritative before touching any code
2. `backend/app/Http/Controllers/Api/ExpenseController.php` — integration point for processRecurring() hook in index()
3. `frontend/src/pages/BudgetPage.tsx` — UI reference: table layout, inline form pattern, InlineError usage; RecurringPage must mirror this
4. `backend/routes/api.php` — add recurring routes inside auth:api group
5. `backend/app/Providers/AppServiceProvider.php` — response()->success() macro; all endpoints must use it

## Critical Anti-Patterns (do NOT repeat these)

- **MISSING try/catch on processRecurring()** → Wrap the call in try/catch in ExpenseController::index(). A bad template must never 500 a normal GET /expenses.
- **Multiple entries per load** → Only create 1 entry per template per trigger. No backfill loop.

## Infrastructure State

- Backend: Laravel 11 on Railway MySQL (PHP 8.x, JWT auth)
- Frontend: React + TypeScript on Vercel
- No local dev server currently running
- Railway MySQL: budgets table NOT yet migrated (non-blocking for Phase 8, but recurring_expenses migration also needs to run post-08-01)

<context>
All 3 Phase 8 plans are written and committed. This is a clean start — no in-progress code. The mental model: recurring_expenses is a template table; actual expense entries land in the existing expenses table (via processRecurring() in ExpenseController). The RecurringPage is the management UI for templates only. Entries appear automatically in the normal expenses list once GET /expenses fires after start_date passes.
</context>

<next_action>
Start with: `/gsd-execute-phase 8` — execute 08-01 first (Wave 1 backend), then 08-02 (Wave 2 frontend types), then 08-03 (Wave 3 UI). All plans are autonomous=true except 08-03 (autonomous=false — requires UI review).
</next_action>
