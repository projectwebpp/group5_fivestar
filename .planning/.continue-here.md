---
context: project
milestone: v2.0
status: supabase-auth-pending-config
last_updated: 2026-05-14T12:18:30Z
---

# Handoff — Supabase Auth Integrated, Awaiting Config

All 8 v2.0 phases complete (20/20 plans). Supabase auth integrated this session.
Code committed. **Blocked on Vercel + Railway configuration.**

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each was discovered through failure.

- [ ] CONSTRAINT: VITE_ prefix required — Vercel currently has `NEXT_PUBLIC_` vars; Vite only exposes `VITE_*`. Auth will fail until renamed.
- [ ] CONSTRAINT: SUPABASE_JWT_SECRET missing — `SupabaseAuthMiddleware` cannot verify any token until this secret is set in Railway.

**Do not proceed until all boxes are checked.**

<current_state>
Supabase auth integration is code-complete and committed. The `/auth` page now calls
`supabase.auth.signInWithPassword` / `signUp` instead of the Laravel auth endpoints.
`SupabaseAuthMiddleware` verifies tokens and auto-provisions local users on first login.

Two blockers remain before production auth works:
1. Vercel env vars have wrong prefix (`NEXT_PUBLIC_` → must be `VITE_`)
2. `SUPABASE_JWT_SECRET` not yet set in Railway (or backend/.env)
</current_state>

<completed_work>

This session (Supabase auth integration):
- ✅ `frontend/src/api/supabase.ts` — Supabase client with env var guard (commit 4b74a21)
- ✅ `frontend/src/api/auth.ts` — Replaced Laravel JWT calls with `supabase.auth.signInWithPassword` / `signUp` (commit 4b74a21)
- ✅ `backend/app/Http/Middleware/SupabaseAuthMiddleware.php` — HS256 JWT verification + auto-provision local user + seed categories on first login (commit 4b74a21)
- ✅ `backend/config/supabase.php` — reads `SUPABASE_JWT_SECRET` from env (commit 4b74a21)
- ✅ `backend/bootstrap/app.php` — registered `supabase.auth` middleware alias (commit 4b74a21)
- ✅ `backend/routes/api.php` — swapped `jwt.auth` + `auth:api` → `supabase.auth` on all protected routes (commit 4b74a21)
- ✅ Error handling hardened — all errors normalised to `{ response: { data: { message } } }` envelope (commit 26d93c6)
</completed_work>

<remaining_work>

All code is done. Only human configuration tasks remain:

### Priority 1 — Fix Vercel env vars (BLOCKING — auth returns "Something went wrong")
Go to: **Vercel → project → Settings → Environment Variables**

| Delete | Add |
|--------|-----|
| `NEXT_PUBLIC_SUPABASE_URL` | `VITE_SUPABASE_URL` = `https://jxvhwfcnjcsqenhpsdfx.supabase.co` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `VITE_SUPABASE_ANON_KEY` = your anon key (`eyJhbGc...` JWT) |

### Priority 2 — Add SUPABASE_JWT_SECRET to Railway (BLOCKING — backend rejects all tokens)
Get from: **Supabase dashboard → Project Settings → API → JWT Settings → JWT Secret**

Add to Railway:
```
SUPABASE_JWT_SECRET = <value from Supabase dashboard>
```

Also update local `backend/.env`:
```
SUPABASE_JWT_SECRET=<value>   # replace 'your-supabase-jwt-secret-here' placeholder
```

### Priority 3 — Redeploy Vercel
Vercel embeds `VITE_*` vars at build time. After step 1, trigger a redeploy:
- Push any commit, OR
- Vercel dashboard → Deployments → Redeploy

### Priority 4 — git push origin main
```bash
git push origin main
```
(26d93c6 is 1 commit ahead of origin/main)

### Priority 5 — Pre-existing deployment tasks (from previous session)
- Run `php artisan migrate --force` on Railway (budgets + recurring_expenses tables)
- Verify Vercel Root Directory = `frontend` (Settings → General)
- Verify `VITE_API_URL` = Railway backend URL in Vercel env vars
</remaining_work>

<decisions_made>

**This session:**
- Supabase auth replaces Laravel JWT auth on the frontend — user chose "bypass Laravel auth entirely"
- Backend still handles data API; `SupabaseAuthMiddleware` verifies Supabase tokens server-side
- `SupabaseAuthMiddleware` auto-provisions a local `users` row + seeds 10 default categories on the first API call from a new Supabase user (bridges Supabase UUID → Laravel integer ID gap)
- `VITE_` prefix required (not `NEXT_PUBLIC_`) — this is Vite, not Next.js
- Supabase URL = base URL only (`https://jxvhwfcnjcsqenhpsdfx.supabase.co`) — no `/rest/v1/` suffix; SDK adds paths internally
- Token verification uses HS256 + Supabase JWT secret (no extra package — pure PHP `hash_hmac`)
</decisions_made>

<blockers>
- **SUPABASE_JWT_SECRET missing**: `SupabaseAuthMiddleware` returns 401 "Token invalid" for all requests until set in Railway
- **Vercel NEXT_PUBLIC_ prefix**: Auth UI returns "Something went wrong" — Supabase client gets `undefined` URL
- **Supabase email confirmation**: If Supabase project has "Confirm email" enabled, new `signUp` won't return a session — user sees "Check your email..." message. Disable auto-confirm in Supabase Auth settings if you want instant access after register
</blockers>

## Required Reading (in order)
1. `backend/app/Http/Middleware/SupabaseAuthMiddleware.php` — understand auto-provision logic before debugging auth issues
2. `frontend/src/api/auth.ts` — Supabase call flow and error normalisation
3. `.planning/STATE.md` — full context on pre-existing deployment blockers

## Infrastructure State
- **Frontend**: Vite + React, deployed to Vercel (group5-fivestar.vercel.app)
- **Backend**: Laravel 11, deployed to Railway
- **Database**: Supabase PostgreSQL (DB_HOST = db.jxvhwfcnjcsqenhpsdfx.supabase.co)
- **Auth**: Supabase Auth (replaces Laravel JWT) — code complete, config pending
- **Supabase project**: jxvhwfcnjcsqenhpsdfx.supabase.co

## What Was Built (full history)

**v2.0 phases (complete):**
- Phase 6: Budget management — /budget, category limits, over-budget warnings (REQ-20/21/22)
- Phase 7: CSV export — Export CSV on /expenses (REQ-23)
- Phase 8: Recurring expenses — /recurring, template CRUD, auto-creation (REQ-24)

**Post-v2.0 this session:**
- Supabase auth integration (replaces Laravel JWT on frontend + new backend middleware)

**Known non-blocking issues:**
- CR-02: 500 on duplicate budget insert
- CR-04: decimal validation missing on expense amount
- WR-01: silent no-op on budget update
- 6 pre-existing test failures (CategoryTest ×2, ExpenseApiTest ×3, ExampleTest ×1)

<context>
All code for Supabase auth is done and committed. The session was focused on wiring /auth to
Supabase after the user provided their Supabase credentials. Key discovery: user had set Vercel
env vars with NEXT_PUBLIC_ prefix (Next.js convention) instead of VITE_ prefix (Vite requirement)
— this caused the generic "Something went wrong" error. Error handling in auth.ts was also
hardened to catch native Errors (network failures, invalid URL) that weren't being normalised
to the expected envelope shape.
</context>

<next_action>
Start with: Go to Vercel → Settings → Environment Variables → rename NEXT_PUBLIC_SUPABASE_URL to
VITE_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY to VITE_SUPABASE_ANON_KEY → then add
SUPABASE_JWT_SECRET to Railway → trigger Vercel redeploy → test /auth.
</next_action>
