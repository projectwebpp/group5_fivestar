export default function ExpenseFormPage({ mode }: { mode: 'create' | 'edit' }) {
  return (
    <div style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      Form ({mode}) — implemented in plan 04-03.
    </div>
  );
}
